test:
  pre:
    - bower install
deployment:
  prod:
    branch: master
    heroku:
      appname: colorwatch
  staging:
    branch: staging
    commands:
      - grunt build
      - grunt buildcontrol:heroku
