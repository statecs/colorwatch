test:
  pre:
    - bower install
deployment:
  prod:
    branch: master
    heroku:
      appname: colorwatch
  staging:
    branch: staging
    commands:
      - git push -f git@heroku.com:colorwatch-staging.git
