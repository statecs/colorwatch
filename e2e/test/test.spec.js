'use strict';

describe('Test View', function() {
  var page;

  beforeEach(function() {
    browser.get('/test');
  });

  it('should calculate new rating when clicking on color combination', function () {});
  it('should add active state to current quesition', function () {});
});
